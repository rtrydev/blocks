#ifndef BLOCKS_VIEWPORT
#define BLOCKS_VIEWPORT

#include "types.h"

Vector3 getViewportPosition();
void setViewportPosition(Vector3 position);

Vector3 getViewportRotation();
void setViewportRotation(Vector3 rotation);

#endif
